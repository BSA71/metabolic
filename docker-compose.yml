services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: metabolic
      POSTGRES_PASSWORD: metabolic_password
      POSTGRES_DB: metabolic
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
